MYSQL_ROOT_PASSWORD=change-me-root-password
MYSQL_DATABASE=community
MYSQL_USER=community
MYSQL_PASSWORD=change-me-community-password

# Spring Boot는 Docker Compose 환경에서 위 MySQL 값을 사용합니다.
# 로컬에서 직접 실행하면 backend/src/main/resources/application.properties의 H2 기본값을 사용합니다.
